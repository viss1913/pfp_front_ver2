# Сообщение для Frontend Разработчика

Привет! 👋

Провели диагностику проблемы с сохранением `telegram_channel_id` для агентов.

## 🔍 Результаты диагностики

### ✅ Бэкенд работает корректно

Запустил тестовый скрипт, который:
1. Авторизуется на `https://pfpbackend-production.up.railway.app/api`
2. Обновляет агента через `PATCH /api/pfp/agents/{id}` с полем `telegram_channel_id`
3. Проверяет сохранение через `GET /api/pfp/agents/{id}`

**Результат:** поле `telegram_channel_id` успешно сохраняется в БД и возвращается в ответе API ✅

### ❌ Проблема на фронтенде

Когда пользователь вводит `telegram_channel_id` в форму редактирования агента и нажимает "Сохранить", это значение **не отправляется на бэкенд**.

## 🛠️ Что нужно исправить на фронтенде

### 1. Проверь форму редактирования агента

Найди компонент/страницу, где редактируется агент. Убедись, что:

- **Есть поле ввода для `telegram_channel_id`** (судя по скриншоту, оно есть - "Telegram Channel ID")
- **Это поле привязано к state/form data**
- **При отправке формы это поле включается в payload запроса**

### 2. Проверь API запрос

Когда нажимаешь "Сохранить", проверь в DevTools → Network:

```http
PATCH https://pfpbackend-production.up.railway.app/api/pfp/agents/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
  "telegram_channel_id": "-100123456789",
  ... другие поля ...
}
```

**Важно:** убедись, что `telegram_channel_id` присутствует в JSON body запроса!

### 3. Пример правильного кода (React/Next.js)

```tsx
// В форме должно быть поле
const [formData, setFormData] = useState({
  first_name: '',
  last_name: '',
  telegram_bot: '',
  telegram_channel: '',
  telegram_channel_id: '', // ← Это поле должно быть!
  // ... другие поля
});

// При отправке
const handleSave = async () => {
  const response = await fetch(`/api/pfp/agents/${agentId}`, {
    method: 'PATCH',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      telegram_bot: formData.telegram_bot,
      telegram_channel: formData.telegram_channel,
      telegram_channel_id: formData.telegram_channel_id, // ← Проверь, что это отправляется!
      // ... остальные поля
    }),
  });
};
```

## 📋 Checklist для проверки

- [ ] Поле `telegram_channel_id` есть в форме
- [ ] Поле связано с state (onChange обновляет значение)
- [ ] При сабмите поле включается в API запрос
- [ ] В DevTools видно, что поле отправляется в body
- [ ] После сохранения данные корректно обновляются

## 🔗 Полезная информация

- **API Endpoint:** `PATCH /api/pfp/agents/{id}`
- **Backend работает:** проверено тестом, поле сохраняется
- **OpenAPI Spec:** поле `telegram_channel_id` описано в `AgentUpdate` schema
- **База данных:** колонка `telegram_channel_id` существует в таблице `agents`

---

Если нужна помощь или есть вопросы - пиши! 🚀

*P.S. От меня (AI assistant) тоже привет! Удачи с фиксом! 😊*
