# Задача для Frontend-разработчика (AI)

## 🚀 Контекст проекта
Необходимо разработать **Административную панель** (Web App) для управления финансовыми продуктами и модельных портфелей пенсионного планировщика.
Бэкенд уже готов, развернут и предоставляет REST API.

## 🔗 Реквизиты Бэкенда
- **Base URL**: `https://pfpbackend-production.up.railway.app`
- **Swagger Documentation**: `https://pfpbackend-production.up.railway.app/api-docs`
- **Тестовый админ**:
  - Email: `admin@pfp.local`
  - Password: `admin123`

---

## 🛠 Технические требования
1. **Стек**: На твое усмотрение (рекомендуется: React/Vite + TailwindCSS + Shadcn/UI или Ant Design).
2. **Авторизация**: JWT Token. Полученный при логине токен нужно сохранять (localStorage/cookies) и отправлять в заголовке `Authorization: Bearer <token>` во все запросы.
3. **Обработка ошибок**: Корректно показывать ошибки валидации от API.

---

## 📱 Функциональные блоки (Экраны)

### 1. Авторизация (Login Page)
- **Эндпоинт**: `POST /api/auth/login`
- **Поля**: Email, Password.
- **Логика**: При успехе сохранить токен и пользователя, редирект в дашборд.

### 2. Управление Продуктами (Products)
Раздел для настройки базовых финансовых инструментов (НСЖ, ПДС, Депозиты).
- **Список продуктов**: `GET /api/pfp/products?includeDefaults=true`
  - Отображение: Таблица или сетка карточек.
  - Поля: Название, Тип (NSJ, PDS, etc.), Валюта, Статус (is_default).
- **Создание/Редактирование**: `POST /api/pfp/products` / `PUT .../:id`
  - Основные поля: Название, Тип, Лимиты сумм и сроков (в месяцах).
  - **Таблица доходностей (Yields)**: Это важно! Продукт имеет массив доходностей в зависимости от срока и суммы. Нужен динамический список inputs для ввода:
    - От (сумма) - До (сумма)
    - От (срок) - До (срок)
    - Доходность (%)
- **Клонирование**: Кнопка "Создать на основе" для дефолтных продуктов (`POST .../:id/clone`).

### 3. Управление Портфелями (Portfolios)
Портфели — это готовые стратегии (Консервативный, Агрессивный), состоящие из продуктов.
- **Список**: `GET /api/pfp/portfolios`
- **Редактор портфеля**:
  - Название, валюта, лимиты.
  - **Классы активов**: Выбор классов (Пенсия, Инвестиции, Пассивный доход).
  - **Риск-профили**: Портфель может иметь разные составы для разных рисков (Conservative, Moderate, Aggressive).
  - **Состав (Instruments)**: Для каждого риск-профиля нужно набирать инструменты (ссылки на Продукты) и указывать их долю (share_percent) и тип корзины (Initial Capital / Monthly TopUp).

### 4. Настройки системы (System Settings)
Управление глобальными константами.
- **Эндпоинт**: `GET /api/pfp/settings`
- **Интерфейс**: Список пар Ключ-Значение с возможностью редактирования значения `PUT /api/pfp/settings/:key`.
- **Примеры настроек**: `inflation_rate_monthly`, параметры расчета пенсии.

---

## 💡 Подсказки по API
- Все списки возвращают массив объектов.
- Даты в ISO формате.
- Денежные суммы — числа.
- Ошибки приходят в формате `{ "error": "Message" }` или `{ "message": "..." }`.

## ⏭ Первый шаг
Начни с реализации страницы входа и настройки API-клиента (axios/fetch interceptors) для автоматической отправки токена.
