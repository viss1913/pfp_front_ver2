import { Navigate } from 'react-router-dom'

/**
 * Templates removed in Content Factory IDE v1.
 * Keep route for old bookmarks → offers.
 */
export default function ContentFactoryTemplates() {
  return <Navigate to="/content-factory/offers" replace />
}
